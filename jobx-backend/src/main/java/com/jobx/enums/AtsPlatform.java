package com.jobx.enums;

public enum AtsPlatform {
    GREENHOUSE,
    LEVER,      // deferred — fetcher stub only
    ASHBY,      // deferred — fetcher stub only
    WORKABLE,   // deferred — fetcher stub only
    UNSUPPORTED // for portals with no clean public API (Workday, BambooHR, etc.)
}
